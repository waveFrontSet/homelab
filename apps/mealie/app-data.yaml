apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie-app-data
  namespace: mealie
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn
