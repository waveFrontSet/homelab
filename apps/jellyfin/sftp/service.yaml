---
apiVersion: v1
kind: Service
metadata:
  name: sftp-service
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: sftp-server
  ports:
    - protocol: TCP
      port: 22
      targetPort: sftp
