apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: traefik
spec:
  interval: 15m
  chart:
    spec:
      chart: traefik
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: traefik
      version: "40.3.0"
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
    strategy:
      name: RetryOnFailure
      retryInterval: 5m
  values:
    providers:
      kubernetesIngress:
        publishedService:
          enabled: true
    additionalArguments:
      - --entrypoints.websecure.http.middlewares=traefik-lan-only@kubernetescrd
    ports:
      web:
        http:
          redirections:
            entryPoint:
              to: websecure
              scheme: https
              priority: 10
              permanent: true
    tlsStore:
      default:
        defaultCertificate:
          secretName: wildcard-external-tls
    ingressRoute:
      dashboard:
        enabled: true
        annotations:
          gethomepage.dev/description: Traefik Dashboard
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Infrastructure
          gethomepage.dev/icon: traefik.png
          gethomepage.dev/name: Traefik
          gethomepage.dev/app: traefik
          gethomepage.dev/pod-selector: ""
        matchRule: Host(`traefik.${EXTERNAL_DOMAIN}`) || Host(`traefik.homelab`)
        entryPoints:
          - websecure
        tls: {}
    service:
      annotations:
        metallb.io/address-pool: ingress-pool
      spec:
        type: LoadBalancer
        ipFamilyPolicy: PreferDualStack
        externalTrafficPolicy: Local
